export const environment = {
    production: false,
    apiUrl: '/api',
    hubUrl: '/hubs/dashboard',
    logging: {
        enabled: true,
        level: 'debug' as 'debug' | 'info' | 'warn' | 'error'
    }
};

